# fogitaboutit

Honors Topics Git Recreation

GP-2.1: Initialize a Repository - init() is now functional, creates full setup for
repo initialization

GP-2.2: Create a Hash Function - hashFile() function implemented, files can now be
hashed for storing in objects (unimplemented)

(GP-2.3): Create BLOB Files - save() function implemented, hashes a file and stores it as a BLOB inside the objects folder;
index unimplemented
